# Hugging Face Spaces runs on CPU, so this pulls the CPU torch build.
#
# Do NOT reuse requirements-cuda.txt here: it would download ~2.5 GB of CUDA
# wheels onto a machine with no GPU, blow the build time, and change nothing
# about the result.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.9.1
torchvision==0.24.1

monai==1.5.2
pydicom>=2.4
numpy>=1.26
pandas>=2.1
openpyxl>=3.1
pillow>=10.0
opencv-python-headless>=4.9
scikit-image>=0.22
scikit-learn>=1.4
matplotlib>=3.8
seaborn>=0.13
pyyaml>=6.0
tqdm>=4.66
altair>=5.0
streamlit>=1.40
