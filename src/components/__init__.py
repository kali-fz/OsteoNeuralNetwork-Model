"""UI components for the ONNM Streamlit interface.

Each component is a self-contained module that renders a specific UI element
via streamlit.components.v1.html.  The rendering pipeline is independent of
the data source so callers can pass any conforming payload.
"""
